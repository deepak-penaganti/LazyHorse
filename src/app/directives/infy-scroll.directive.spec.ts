import { InfyScrollDirective } from './infy-scroll.directive';

describe('InfyScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new InfyScrollDirective();
    expect(directive).toBeTruthy();
  });
});
